import React from "react";

export default function AdminPage() {
  return (
    <div>
      <h1>
        Welcome to Admin management console. There you can, as an admin, not
        only view all the products, orders and users, but also remove products,
        delete orders and suspend users.
      </h1>
    </div>
  );
}
