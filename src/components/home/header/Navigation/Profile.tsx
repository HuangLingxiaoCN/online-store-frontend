import { GenericProps } from '../../../../Types';
import LinkButton from '../../../UI/linkButton/LinkButton'

export default function Profile(props: GenericProps) {
  return (
    <div style={{ marginRight: "10px" }}>
      <LinkButton>Profile</LinkButton>
    </div>
  );
}
